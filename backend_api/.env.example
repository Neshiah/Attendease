PORT=4000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=student_attendance_rewards
JWT_SECRET=change_this_secret
POINTS_PER_PRINTED_PAGE=10

# Preferred Gmail verification email delivery.
# Enable Google 2-Step Verification, then create a 16-character App Password.
GMAIL_USER=
GMAIL_APP_PASSWORD=
MAIL_FROM_NAME=Attendease

# Optional generic SMTP fallback.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
